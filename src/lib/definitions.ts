export interface ICartItem {
  imgSrc: string;
  combo: string;
  title: string;
  price: number;
}

export interface IHeaderNotifyItem {
  src: string;
  title: string;
  description: string;
  isRead: boolean;
}
